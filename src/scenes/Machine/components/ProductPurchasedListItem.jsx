import React from 'react'

const ProductPurchasedListItem = ({ product }) => (
    <div className="product-list-item">
        <h4>{product.name}</h4>
    </div>
)

export default ProductPurchasedListItem